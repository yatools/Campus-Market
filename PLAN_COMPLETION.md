# 梧桐墙单校试运营生产化改造完成度

更新日期：2026-07-13

## 结论

计划中的生产代码、迁移、部署编排、关键业务闭环和自动化回归已基本落地；原型已隔离在 `legacy/`，不进入生产镜像。当前不能直接宣告“已上线”，因为 DNS/TLS、真实校园 SMTP、同机 Staging 恢复演练和 100 并发性能数据必须在目标 Ubuntu 服务器上取得，不能用本机 SQLite 或模拟结果替代。

状态定义：

- **已完成**：代码存在，并有自动测试、静态检查或配置校验证据。
- **待环境验收**：实现与脚本已提供，但必须在目标服务器或真实外部服务上执行。
- **已知偏差**：功能可用，但内部实现没有完全采用计划中的最细粒度建模。

## 架构、数据与公共接口

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| Vue 3 + TypeScript + Vite、FastAPI、SQLAlchemy 2、PostgreSQL 16、Alembic | 已完成 | `frontend/`、`backend/app/`、`docker-compose.yml`、`backend/alembic/` |
| API `/api/v1`、统一分页与错误结构、请求 ID | 已完成 | 路由统一前缀；异常处理覆盖验证错误、HTTP 错误和未知 500 |
| Argon2id、安全 Cookie、CSRF、7/30 天会话、会话轮换、当前/全部设备退出 | 已完成 | `security.py`、`deps.py`、认证和会话轮换回归测试 |
| 校邮域、验证码哈希/10 分钟/单次、邮箱与 IP 限流、邮件队列 | 已完成 | `auth.py`、`worker.py`；接口不返回验证码，无公开邮件日志 |
| CampusIdentity、Role、AccountStatus、ContentStatus、IdentityMode | 已完成 | 独立字段与权限依赖；限权账号仍可管理自身和删除内容，但不能继续发布互动 |
| 统一内容、附件、评论、反应、收藏、举报、案件、通知、审计 | 已完成 | 数据模型和外键；不存在对象不能回帖的回归测试 |
| 两级回帖、匿名线程内稳定/跨线程隔离、幂等互动 | 已完成 | 回复目标回归、数据库唯一约束和 `ON CONFLICT DO NOTHING` |
| 图片上传、真实格式/MIME/像素/大小校验、EXIF 清除、缩略图、持久卷 | 已完成 | 上传安全回归覆盖 SVG 与 MIME 伪装 |
| Router、Pinia、统一 API 客户端、OpenAPI 生成类型、Markdown 净化 | 已完成 | `frontend/src/generated/api.d.ts` 已生成并在认证表单中使用 |
| 内容编辑历史 | 已完成 | `content_revisions`、迁移 `0002`、作者/审核员历史接口与回归测试 |

## 功能模块

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| 用户后台：资料头像、校邮、密码、会话、隐私、注销、内容/收藏/通知/举报/申诉 | 已完成 | 内容支持类型/状态筛选和分页；举报与申诉拆分为标准分页接口 |
| 树洞：详情内容、回帖、搜索、热榜、收藏、举报、浏览、编辑记录、软删、过期 | 已完成 | 搜索修正为真实总数/分页；热榜改为聚合查询；根路由 `noindex` |
| 车队：车队、场次、成员历史、容量锁、签到/请假、转让/移除、取消、评价、提醒 | 已完成 | 新增场次创建/改期/取消和成员历史接口及 UI；并发路径使用行锁/唯一约束 |
| 问答与手册：回答、评论、采纳、悬赏原子转移、草稿、分类、收藏、加精奖励 | 已完成 | 禁止自问自答刷采纳经验；拒绝/删除未结算问题时退款 |
| 课程评价目录、每班次一次、匿名聚合、少于 5 条隐藏、更正审计 | 已知偏差 | 功能已完成；课程与教师当前以唯一“课程名 + 教师”目录项建模，未拆成独立教师表 |
| 二手市场：线下面交、图片/价格/地点/状态、收藏、上下文私信、举报不自动冻结 | 已完成 | 无支付/担保路径；举报只建审核案件 |
| 活动与失物：容量报名、退出/取消通知、申请—确认—完成 | 已完成 | 活动容量行锁；失物认领与决定行锁；合法状态下支持编辑 |
| 观察台与治理：先审后发、隐私打码、指定回应、脱敏处罚、本人申诉 | 已完成 | 管理员也不能替指定账号覆盖回应；处罚/申诉均有责任人校验 |
| 私信、公告、通知 | 已完成 | 会话未读、拉黑、举报、双层限流、陌生人开关、上下文权限、SSE 刷新、强提醒邮件队列 |
| 管理后台 | 已完成 | 审核内容预览、举报理由、用户/限权/角色、处罚、申诉、课程、公告、设置、反馈、备份、审计 |
| 可配置风险规则 | 已完成 | `risk_words` 支持 JSON 的 `review`/`reject` 规则；设置变更写审计日志 |

## 部署与运维

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| 生产 `.env.example`、Compose、Nginx TLS/安全头、迁移、首管理员、部署/回滚 | 已完成 | 生产配置拒绝示例占位值和缺失项；CI 校验生产与 Staging Compose |
| 独立任务服务、PostgreSQL advisory lock、邮件重试、提醒、过期与清理 | 已完成 | `worker.py`，无 Redis |
| 存活/就绪、结构化日志、请求 ID、慢请求、邮件失败统计、生产关闭文档 | 已完成 | 管理概览提供待发/失败邮件数；日志使用严格 JSON 编码 |
| 备份、最近 7 份、安全下载、恢复脚本 | 已完成 | 备份含数据库、上传文件与 `SHA256SUMS`；恢复先校验再停服务，并验证数据库/就绪状态 |
| 30 天注销清理 | 已完成 | 会话立即失效；个人字段、私信正文与通知按任务清理；公共作者显示“已注销用户” |
| 同机独立 Staging | 已完成（脚本）/待环境验收 | 独立 project、数据库和 volumes，仅监听 `127.0.0.1:8080`；见 README |

## 测试与上线验收

| 项目 | 状态 | 当前结果 |
| --- | --- | --- |
| 后端 pytest、权限/状态/事务/模块回归 | 已完成 | 29 条测试通过；覆盖认证、内容、车队、问答、市场、活动、失物、治理、私信、任务和备份 |
| 前端类型、Vitest、构建 | 已完成 | TypeScript、2 个单测、Vite 生产构建通过 |
| Playwright 桌面/移动与主流引擎 | 已完成 | Windows：Chromium 桌面/移动、WebKit 共 9 条通过；Linux CI 额外运行 Firefox |
| 迁移一致性 | 已完成 | SQLite 实际执行 `upgrade head` 与 `alembic check`；CI 在 PostgreSQL 16 重复验证 |
| 依赖漏洞 | 已完成 | `pip-audit`：无已知漏洞；`npm audit`（含开发依赖）：0 漏洞 |
| 100 并发与 P95 | 待环境验收 | `mixed_workload.js` 已固化 80 读 + 20 写、P95 和错误率阈值，必须在目标 Staging 执行 |
| DNS/TLS/SMTP、备份恢复实操 | 待环境验收 | 脚本与步骤已提供，必须使用真实域名、校园邮件和异地备份执行 |

## 上线闸门

只有以下项目全部有实际记录后才能开放生产注册：

1. 在 Ubuntu 24.04 上执行生产和 Staging Compose 配置检查及镜像构建。
2. 用真实校园邮箱完成注册、重置密码、强提醒和车队提醒收发。
3. 在 Staging 生成备份，下载异地，执行恢复，并复核内容与上传文件。
4. 执行 `mixed_workload.js`，确认读 P95 `<500 ms`、写 P95 `<800 ms`、错误率 `<1%`。
5. 完成真实 DNS/TLS、手机与桌面人工可访问性检查，再初始化空生产库和首位管理员。
