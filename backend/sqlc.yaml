version: "2"
sql:
  - engine: "postgresql"
    schema: "internal/database/migrations"
    queries: "queries"
    gen:
      go:
        package: "dbgen"
        out: "internal/dbgen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
