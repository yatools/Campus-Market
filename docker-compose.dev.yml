services:
  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address :9001
    environment:
      # 不再提供 minioadmin 默认值：漏配就应该启动失败，而不是静默用弱口令跑起来。
      MINIO_ROOT_USER: ${S3_ACCESS_KEY_ID:?请在 .env 中设置 S3_ACCESS_KEY_ID}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_ACCESS_KEY:?请在 .env 中设置 S3_SECRET_ACCESS_KEY}
    ports:
      # 绑定回环地址：docker 的端口发布会绕过 ufw/firewalld，
      # 之前这两个端口对整个局域网开放。
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:9000/minio/health/ready"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks: [internal]

  minio-init:
    image: minio/mc:RELEASE.2025-04-16T18-13-26Z
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        attempt=0;
        until
          mc alias set local http://minio:9000 "$${S3_ACCESS_KEY_ID}" "$${S3_SECRET_ACCESS_KEY}" &&
          mc mb --ignore-existing local/$${S3_PUBLIC_BUCKET} &&
          mc version enable local/$${S3_PUBLIC_BUCKET} &&
          mc mb --ignore-existing local/$${S3_PRIVATE_BUCKET} &&
          mc version enable local/$${S3_PRIVATE_BUCKET} &&
          mc mb --ignore-existing local/$${S3_BACKUP_BUCKET} &&
          mc version enable local/$${S3_BACKUP_BUCKET} &&
          mc anonymous set download local/$${S3_PUBLIC_BUCKET};
        do
          attempt=$$((attempt + 1));
          if [ "$$attempt" -ge 30 ]; then echo "MinIO initialization failed after 30 attempts" >&2; exit 1; fi;
          sleep 1;
        done
    env_file: ${ENV_FILE:-.env}
    networks: [internal]

  migrate:
    image: wutong-api:dev
    build: ./backend
    environment:
      ENVIRONMENT: development
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      PUBLIC_ORIGIN: http://127.0.0.1:8080
      COOKIE_SECURE: "false"
      S3_ENDPOINT: http://minio:9000
      S3_PUBLIC_BASE_URL: http://127.0.0.1:9000/${S3_PUBLIC_BUCKET}
      SMTP_HOST: ""
      SMTP_USERNAME: ""
      SMTP_PASSWORD: ""
      SMTP_FROM: ""

  api:
    image: wutong-api:dev
    build: ./backend
    environment:
      ENVIRONMENT: development
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      PUBLIC_ORIGIN: http://127.0.0.1:8080
      TRUSTED_HOSTS: 127.0.0.1,localhost
      COOKIE_SECURE: "false"
      DOCS_ENABLED: "true"
      S3_ENDPOINT: http://minio:9000
      S3_PUBLIC_BASE_URL: http://127.0.0.1:9000/${S3_PUBLIC_BUCKET}
      SMTP_HOST: ""
      SMTP_USERNAME: ""
      SMTP_PASSWORD: ""
      SMTP_FROM: ""
    depends_on:
      minio-init:
        condition: service_completed_successfully

  worker:
    image: wutong-api:dev
    build: ./backend
    environment:
      ENVIRONMENT: development
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      PUBLIC_ORIGIN: http://127.0.0.1:8080
      COOKIE_SECURE: "false"
      S3_ENDPOINT: http://minio:9000
      S3_PUBLIC_BASE_URL: http://127.0.0.1:9000/${S3_PUBLIC_BUCKET}
      SMTP_HOST: ""
      SMTP_USERNAME: ""
      SMTP_PASSWORD: ""
      SMTP_FROM: ""
    depends_on:
      minio-init:
        condition: service_completed_successfully

  web:
    image: wutong-web:dev
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports: !override
      - "127.0.0.1:8080:8080"
    volumes: !override
      - ./deploy/staging.conf.template:/etc/nginx/templates/default.conf.template:ro

volumes:
  minio_data:
